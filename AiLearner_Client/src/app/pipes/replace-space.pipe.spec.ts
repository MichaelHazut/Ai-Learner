import { ReplaceSpacePipe } from './replace-space.pipe';

describe('ReplaceSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
